# Тестовое задание для Windi - web chat

Прототип чат-приложения на React 18+ и TypeScript, демонстрирующий production-подход к разработке frontend-приложений.

## Запуск проекта

### Установка зависимостей

```bash
npm install
```

### Разработка

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### Сборка

```bash
npm run build
```

### Предпросмотр production сборки

```bash
npm run preview
```


## 🚀 Технологический стек

- **React 18+** — современная библиотека для построения UI
- **TypeScript** — типизированный JavaScript для надежности кода
- **Tailwind CSS** — utility-first CSS фреймворк для быстрой стилизации
- **Zustand 5** — легковесный state management
- **react-virtuoso** — современная виртуализация больших списков, гибкая и не тяжеловесная
- **Jest + React Testing Library** — тестирование
- **Vite 6** — быстрый сборщик и dev-сервер

## 📁 Архитектура проекта

Проект организован по методологии **Feature-Sliced Design (FSD)**, которая обеспечивает:

- **Масштабируемость** — легко добавлять новые фичи
- **Изоляцию** — каждый слой имеет четкие границы
- **Переиспользование** — компоненты и логика разделены по уровням
- **Предсказуемость** — понятная структура для всей команды
- **Актуальность** — современный подход к фронтенд разработке

## Функциональность

### Основные возможности

- Список чатов с аватарами и последними сообщениями
- История сообщений (5000+ сообщений) с виртуализацией
- Отправка сообщений с оптимистичным обновлением UI
- Real-time обновления через mock WebSocket
- Выравнивание сообщений (свои справа, чужие слева)
- Адаптивный интерфейс

### Производительность

- **Виртуализация списка** — рендерится только видимая часть сообщений // TODO: докрутить рендеринг только видимой части сообщений соединив с ленивой загрузкой
- **Мемоизация** — использование `useMemo` для сортировки сообщений
- **Оптимистичные обновления** — мгновенная реакция на действия пользователя
- **Ленивая загрузка** — сообщения загружаются только при выборе чата

## Тестирование

Проект включает юнит-тесты для:

- **Store (Zustand)** — тестирование бизнес-логики
- **UI компоненты** — тестирование рендеринга и взаимодействий

Запуск тестов:

```bash
npm test
npm run test:watch
npm run test:coverage
```

## Обоснование технических решений

### Почему Zustand?

**Zustand** выбран как state management решение по следующим причинам:

#### 1. Простота и минимализм

Zustand предоставляет минимальный API без избыточного boilerplate кода

#### 2. Производительность

- **Нет лишних ре-рендеров** — компоненты подписываются только на нужные части состояния
- **Нет middleware по умолчанию** — меньше накладных расходов
- **Меньший bundle size** (~1KB vs ~10KB для Redux)

#### 3. TypeScript поддержка

Отличная типизация из коробки без дополнительных настроек

#### 4. Гибкость

- Можно использовать вне React компонентов
- Легко интегрируется с другими библиотеками
- Поддержка middleware при необходимости


#### 5. Почему Zustand для чата?

Для чат-приложения Zustand идеален, потому что:

- **Простая структура состояния** — чаты, сообщения, активный чат
- **Нет сложной логики** — не нужны middleware, а если возникнет необходимость, то легко можно добавить
- **Real-time обновления** — легко интегрируется с WebSocket (не применяется в тестовом)
- **Оптимистичные обновления** — простые синхронные обновления состояния

### Почему Feature-Sliced Design?

FSD обеспечивает:

1. **Масштабируемость** — легко добавлять новые фичи без рефакторинга
2. **Изоляцию** — изменения в одной фиче не влияют на другие
3. **Переиспользование** — компоненты из `shared` и `entities` используются везде
4. **Понятность** — структура интуитивна для новых разработчиков

### Почему react-virtuoso ?

Для списка из 5000+ сообщений виртуализация критична:

- **Производительность** — рендерится только ~20-30 видимых элементов
- **Память** — не хранятся DOM-узлы для всех сообщений
- **Плавность** — нет лагов при скролле
- **Современность** — активно поддерживается, легче и быстрее чем остальные популярные аналоги
- **Гибкость** — работает с любыми контейнерами, не только с фиксированными размерами
- **TypeScript** — отличная типизация из коробки


## Real-time обновления

Реализован mock WebSocket, который:

- Подключается после загрузки чатов
- Отправляет новые сообщения каждые 3-7 секунд
- Автоматически обновляет активный чат
- Обновляет последнее сообщение в списке чатов


## Будущие улучшения

- [ ] Пагинация сообщений вместо загрузки всех сразу
- [ ] При скролле списка сообщений, размонтировать все участки вне зоны видимости
- [ ] Сделать виртуализацию списка чатов
- [ ] Увеличить кол-во тестов
- [ ] Добавить роутинг для чатов
- [ ] Отображение статуса отправки
- [ ] Отрефакторить виртуализированный список
- [ ] Отрефакторить стор
